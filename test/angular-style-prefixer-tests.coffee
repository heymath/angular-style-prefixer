chai = require 'chai'
sinon = require 'sinon'

{Angularstyle-prefixer} = require '../src/angular-style-prefixer'

chai.should()

describe 'angular-style-prefixer', ->
  it 'should do something', ->
    true.should.equal true